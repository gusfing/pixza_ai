import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Sign In | Pixza Studio", description: "Sign in to your Pixza Studio account to access AI image and video generation.", path: "/auth/signin", noIndex: true });
