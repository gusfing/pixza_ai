import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Create Account | Pixza Studio", description: "Create a free Pixza Studio account and start generating AI images and videos today.", path: "/auth/signup" });
