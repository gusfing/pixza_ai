import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Free Magic Eraser — Remove Objects from Photos with AI | Pixza Studio", description: "Paint over anything to remove it. AI fills the area seamlessly. Free, no sign-in required.", path: "/tools/magic-eraser" });
