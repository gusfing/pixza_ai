import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Join the Waitlist — Pixza Studio Early Access", description: "Join the Pixza Studio waitlist for early access and 50% off all paid plans forever.", path: "/waitlist" });
