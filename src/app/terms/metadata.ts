import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Terms of Service | Pixza Studio", description: "Pixza Studio terms of service — the rules and guidelines for using our platform.", path: "/terms" });
