import { createMetadata } from "@/lib/metadata";
export const metadata = createMetadata({ title: "Batch Image Editor — Apply AI to 50 Images at Once | Pixza Studio", description: "Upload up to 50 images and apply the same AI operation to all of them at once. Free.", path: "/batch" });
